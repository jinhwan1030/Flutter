import 'package:flutter/material.dart';

class RouteProfile extends StatelessWidget {
  const RouteProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Profile Tab"),
    );
  }
}
